# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

set $display0 "eDP-1"
set $display1 "DP-2-1"
set $display2 "DP-2-2"
set $i3fontinput "pango:Terminus for Powerline 08"
set $i3rofimark "~/.local/bin/i3-rofi-mark"

# define names for workspaces
set $ws1    "1:  "
set $ws2    "2:  "
set $ws3    "3:  "
set $ws4    "4:  "
set $ws5    "5:  "
set $ws6    "6:  "
set $ws7    "7:  "
set $ws8    "8:  "
set $ws9    "9:  "

# chose a FreeType font, such as:
font $i3fontinput

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
#bindsym $mod+Return exec i3-sensible-terminal
bindsym $mod+Return exec gnome-terminal
# start a filemanager
bindsym $mod+Shift+Return exec --no-startup-id nautilus

# kill focused window
bindsym $mod+Shift+q kill

bindsym $mod+space exec "rofi -show drun -o 85"
bindsym $mod+Shift+a exec "rofi -combi-modi window,drun -show combi -modi combi"
bindsym $mod+Shift+p exec passmenu
bindsym $mod+g exec "gentoken.sh"
bindsym $mod+F2 exec --no-startup-id $HOME/.screenlayout/office.sh
bindsym $mod+F4 exec --no-startup-id $HOME/bin/mutemic.sh
bindsym $mod+F6 exec $HOME/.i3/monitor_layout.sh
bindsym $mod+F7 exec $HOME/.i3/rofi-randr
bindsym $mod+F8 exec --no-startup-id "setxkbmap -layout us -variant altgr-intl -option grp:shift_shift_toggle"
bindsym $mod+F9 exec --no-startup-id "setxkbmap -layout fr -option grp:shift_shift_toggle"
bindsym $mod+F10 exec --no-startup-id $HOME/bin/i3-touchpad enable
bindsym $mod+F11 exec --no-startup-id $HOME/bin/i3-touchpad disable
bindsym $mod+d exec i3-msg workspace $ws2; exec "firefox-wayland -P work"
bindsym $mod+shift+m exec "pavucontrol"
# Take a Screenshot using scrot.
bindsym Print exec "scrot '%Y%m%d-%H%M%S_$wx$h.png' -e 'mv $f ~/shots/'"

bindsym $mod+Shift+x exec i3-msg workspace $ws3; exec urxvt256c-ml -title Ranger -name Ranger -e zsh -c "ranger ~/"
bindsym $mod+Control+k exec gnome-terminal -t "Get Kerberos Ticket" -- /bin/zsh -i -c "kinit && klist && sleep 2"

bindsym $mod+Shift+u exec gnome-terminal -t IRC -- /bin/zsh -i -c "mux IRC" &
bindsym $mod+Control+u exec gnome-terminal -t MAIL -- /bin/zsh -i -c "mux redhat" &
bindsym $mod+Control+d exec gnome-terminal -t DEVEL -- /bin/zsh -i -c "mux DEVEL" &

# screen locker (first move to "safe" workspace without any chat app)
bindsym Control+$mod+l exec $HOME/bin/i3lockmore -d --lock-icon --image-fill $HOME/Pictures/mnmlist3.jpg --dpms 5
bindsym $mod+Shift+s exec $HOME/.i3/./rofi-power

# compton
bindsym $mod+b exec --no-startup-id pkill compton; exec notify-send -t 2000 'Compton Off'
bindsym $mod+Shift+b exec --no-startup-id compton -b --config ~/.i3/compton.conf; exec notify-send -t 2000 'Compton On'

# redshift
bindsym Control+$mod+b exec --no-startup-id pkill redshift; exec notify-send -t 2000 'RedShift Off'

# Emacs
bindsym Control+$mod+n exec --no-startup-id "gnome-terminal -t Emacs -- /bin/zsh -i -c 'emacsclient -nw'"
bindsym $mod+n exec --no-startup-id "gnome-terminal -t Emacs -- /bin/zsh -i -c 'emacs -nw'"

# pulse audio volume control
bindsym XF86AudioLowerVolume exec /usr/bin/pactl set-sink-volume @DEFAULT_SINK@ -5%; exec /usr/bin/killall -USR1 i3status
bindsym XF86AudioRaiseVolume exec /usr/bin/pactl set-sink-volume @DEFAULT_SINK@ +5%; exec /usr/bin/killall -USR1 i3status
bindsym XF86AudioMute exec /usr/bin/pactl set-sink-mute @DEFAULT_SINK@ toggle; exec /usr/bin/killall -USR1 i3status
bindsym XF86MonBrightnessUp exec sudo /usr/bin/light -A 10
bindsym XF86MonBrightnessDown exec sudo /usr/bin/light -U 5

# mark container
#bindsym $mod+i exec i3-input -f $i3fontinput -F 'mark %s' -P 'Mark name: '
#bindsym $mod+Shift+o exec i3-input -f $i3fontinput -F '[con_mark=%s] focus' -P 'Go to mark: '
bindsym $mod+i exec ~/.local/bin/i3-rofi-mark mark
bindsym $mod+shift+o exec ~/.local/bin/i3-rofi-mark goto

# turn off the screen
bindsym $mod+shift+z exec "sleep 2; xset dpms force off"

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

hide_edge_borders both
new_window pixel 0
new_float pixel 0
default_border pixel 0
default_floating_border pixel 0
bindsym $mod+t border normal
bindsym $mod+y border pixel 1
bindsym $mod+u border none

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+H move left
bindsym $mod+Shift+J move down
bindsym $mod+Shift+K move up
bindsym $mod+Shift+L move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym Control+$mod+h split h
# split in vertical orientation
bindsym Control+$mod+v split v

# split orientation
#bindsym $mod+v layout toggle split

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+Shift+Tab focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

bindsym $mod+p bar mode toggle
#bindsym $mod+Shift+l bar mode dock
bindsym $mod+z workspace prev
bindsym $mod+x workspace next
bindsym $mod+c workspace back_and_forth

# focus the child container
#bindsym $mod+d focus child

workspace_auto_back_and_forth yes
focus_follows_mouse no

workspace_layout default

# switch to workspace
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace 10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace 10

#bindsym $mod+F1 focus output $display0
#bindsym $mod+F2 focus output $display1
#bindsym $mod+F3 focus output $display2

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 1 px or 1 ppt
        bindsym k resize grow height 1 px or 1 ppt
        bindsym l resize shrink height 1 px or 1 ppt
        bindsym semicolon resize grow width 1 px or 1 ppt

        # same bindings, but for the arrow keyR
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

# Create Log out, Reboot, Poweroff bindings
#set $screen_setup SCREEN (l) Laptop Only, (h) Office
#mode "$screen_setup" {
    #bindsym l exec --no-startup-id $HOME/.screenlayout/laptop.sh, mode "default"
    #bindsym h exec --no-startup-id $HOME/.screenlayout/home.sh, mode "default"

    ## back to normal: Enter or Escape
    #bindsym Return mode "default"
    #bindsym Escape mode "default"
#}
#bindsym $mod+Shift+S mode "$screen_setup"

bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
  position top
  font pango:Terminus for Powerline 08
  # height 30
  status_command ~/bin/i3status-rs ~/.i3/.i3status-rust.toml
  tray_output eDP-1
  mode dock
  separator_symbol "  "
  workspace_buttons yes
  colors {
    separator #444444
    background #333333
    statusline #ffffff
    focused_workspace #d70a53 #d70a53 #ffffff
    active_workspace #333333 #333333 #888888
    inactive_workspace #333333 #333333 #888888
    urgent_workspace #eb709b #eb709b #ffffff
  }
}

# Workspaces
workspace $ws1 output $display1
workspace $ws2 output $display1
workspace $ws3 output $display1
workspace $ws4 output $display1
workspace $ws5 output $display1
workspace $ws6 output $display1
workspace $ws7 output $display1
workspace $ws8 output $display1
workspace $ws9 output $display0
workspace 10 output $display2

# scratchpad
bindsym $mod+m move scratchpad
bindsym $mod+o scratchpad show

## Make the currently focused window a scratchpad
#bindsym $mod+Shift+minus move scratchpad
## Show the first scratchpad window
#bindsym $mod+minus scratchpad show

# workspace assignment (use "xprop")
assign [class="^Firefox$"] $ws2
assign [class="^Nautilus$"] $ws3
assign [class="^Evince$"] $ws3
assign [class="^Zathura$"] $ws3
assign [class="^Stardict$"] $ws8

# i3-gaps
gaps inner 05
gaps outer 05
smart_gaps on

set $mode_gaps Gaps: (o)uter, (i)nner
set $mode_gaps_outer Outer Gaps: k|j|0 (local), Up +|-|Ctrl+0 (global)
set $mode_gaps_inner Inner Gaps: k|j|0 (local), Down +|-|Ctrl+0 (global)
bindsym $mod+Shift+g mode "$mode_gaps"

mode "$mode_gaps" {
        bindsym o      mode "$mode_gaps_outer"
        bindsym i      mode "$mode_gaps_inner"
        bindsym Return mode "$mode_gaps"
        bindsym Escape mode "default"
}

mode "$mode_gaps_outer" {
        bindsym k  gaps outer current plus 5
        bindsym j gaps outer current minus 5
        bindsym 0     gaps outer current set 0

        bindsym Up  gaps outer all plus 5
        bindsym Down gaps outer all minus 5
        bindsym Ctrl+0     gaps outer all set 0

        bindsym Return mode "$mode_gaps"
        bindsym Escape mode "default"
}
mode "$mode_gaps_inner" {
        bindsym k  gaps inner current plus 5
        bindsym j gaps inner current minus 5
        bindsym 0     gaps inner current set 0

        bindsym Up  gaps inner all plus 5
        bindsym Down gaps inner all minus 5
        bindsym Ctrl+0     gaps inner all set 0

        bindsym Return mode "$mode_gaps"
        bindsym Escape mode "default"
}

# custom window settings
#for_window [class="^.*"] border none; title_format ">_ %title "
for_window [class=".*"] title_format ">_ %title "
for_window [class="^.*"] border pixel 0
for_window [window_role="pop-up"] floating enable; border pixel 0, move absolute center
for_window [window_role="task_dialog"] floating enable; border pixel 0
for_window [window_role="bubble"] floating enable; border pixel 0
for_window [window_role="Preferences"] floating enable; border pixel 0
for_window [title="- Shutter"] floating enable; border pixel 0
for_window [class="Gnome-disks"] floating enable; border pixel 0
for_window [class="URxvt"] border pixel 0
for_window [class="(?i)pavucontrol"] floating enable; move position mouse
for_window [instance="Ranger"] mark ranger
for_window [title=".*IRC$"] mark irc
for_window [title=".*MAIL$"] mark mail
for_window [title=".*DEVEL$"] mark dev
for_window [class="Firefox"] mark web
# for_window [instance="Wiki"] move scratchpad
#for_window [instance="Mail"] resize set 1400 1000, move position center
#for_window [instance="Mail"] move scratchpad

bindsym $mod+F5 [class="Firefox"] move workspace current

# start the session
exec --no-startup-id xset -b &
exec --no-startup-id xset dpms 600 1200 2000 &
exec --no-startup-id ~/bin/keyboard
exec /usr/bin/dunst
exec --no-startup-id compton -b --config ~/.i3/compton.conf
exec clipit -d -n
# exec_always --no-startup-id pgrep redshift || redshift -l 48.864716:2.349014 -t 5700:3600 -g 0.8 -m randr -v 2>&1 | tee -a ~/tmp/redshift.log
exec --no-startup-id /usr/bin/udiskie -anT &
#exec_always feh --bg-scale '/home/gchamoul/Pictures/Discovering-space2-166320056.jpg'
exec_always feh --bg-scale '/home/gchamoul/Pictures/wall.png'
exec seapplet
# exec --no-startup-id urxvt256c-ml -title Wiki -name Wiki -e zsh -c "vim -c VimwikiIndex" &
exec --no-startup-id xrdb -merge ~/.Xresources &
exec --no-startup-id ~/.i3/autostart

# Colors                #Border #backg  #text   #indicator
# class                 border  backgr. text    indicator child_border
client.focused          #333333 #333333 #ffffff #2e9ef4